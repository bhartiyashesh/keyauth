---
phase: 07-faceid-capability-tokens
plan: 08
type: execute
wave: 6
depends_on:
  - "07-01"
  - "07-02"
  - "07-03"
  - "07-04"
  - "07-05"
  - "07-06"
  - "07-07"
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/phases/07-faceid-capability-tokens/07-VALIDATION.md
  - .planning/phases/07-faceid-capability-tokens/07-QA-CHECKLIST.md
  - KeyAuthTests/CodeApprovalViewTests.swift
autonomous: true
requirements:
  - FIDO-08
  - FIDO-13
  - FIDO-18
  - FIDO-19

must_haves:
  truths:
    - ".planning/REQUIREMENTS.md Traceability rows for FIDO-01..FIDO-17 flip from 'Pending' to 'Complete (automated)' OR 'Complete (unit: <TestClass.testName>) / Manual QA pending <2-DEV-NN>' using the Phase 6 hybrid vocabulary"
    - ".planning/REQUIREMENTS.md Traceability rows for FIDO-18 and FIDO-19 flip to 'Manual QA pending 2-DEV-TW-01' (FIDO-18) and 'Manual QA pending 2-DEV-TW-02' (FIDO-19 — extension diff check)"
    - "KeyAuthTests/CodeApprovalViewTests.swift is created with (a) a FIDO-08 grep test that asserts `TrustWindowManager.shared.mint()` appears in CodeApprovalView.swift AFTER the authenticate-success guard, and (b) a FIDO-13 grep test that asserts `startAutoRefresh` does NOT appear anywhere in CodeApprovalView.swift"
    - "VALIDATION.md §Per-Task Verification Map is populated (or replaced) with rows citing the final Plan IDs, task IDs, FIDO IDs, Threat IDs, and the concrete `xcodebuild test -only-testing:KeyAuthTests/<Class>/<method>` commands for every automated row"
    - "VALIDATION.md §Wave 0 Requirements bullet for the fixtures file is rewritten to reference KeyAuthTests/Fixtures/CodeRequestFixtures.swift (the shipped filename) — the placeholder 'TrustWindowFixtures.swift' is removed"
    - "VALIDATION.md `nyquist_compliant: true` frontmatter flag is set (every FIDO has either an automated test or a manual-QA row; no FIDO is orphaned)"
    - "07-QA-CHECKLIST.md is created with at minimum two manual-QA items: 2-DEV-TW-01 (FIDO-18 toast-visible-after-sheet on physical device) and 2-DEV-TW-02 (FIDO-19 `extension/` source unchanged — confirmed by running `git diff main...HEAD -- extension/` and observing zero diff)"
    - ".planning/STATE.md is updated: current_focus points at Phase 7; last_activity = today's date; a Phase 7 decisions block summarizes the key decisions the executors landed on"
    - "Full KeyAuthTests suite exits 0 after all additions"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Phase 7 FIDO-NN traceability flipped from Pending to Complete (automated|unit|Manual QA pending) per Phase 6 hybrid vocabulary"
      contains: "FIDO-01 | Phase 7 | Complete (automated)"
    - path: ".planning/phases/07-faceid-capability-tokens/07-VALIDATION.md"
      provides: "Per-Task Verification Map populated + nyquist_compliant: true + fixture filename aligned with shipped CodeRequestFixtures.swift"
    - path: ".planning/phases/07-faceid-capability-tokens/07-QA-CHECKLIST.md"
      provides: "Manual QA checklist — 2-DEV-TW-01 (FIDO-18) + 2-DEV-TW-02 (FIDO-19)"
    - path: "KeyAuthTests/CodeApprovalViewTests.swift"
      provides: "Grep-based tests for FIDO-08 (mint call present) + FIDO-13 (startAutoRefresh absent)"
      exports: ["CodeApprovalViewTests"]
    - path: ".planning/STATE.md"
      provides: "Phase 7 position + decisions update"
  key_links:
    - from: "REQUIREMENTS.md Traceability"
      to: "Automated test coverage produced by Plans 02-07"
      via: "Complete (automated) vocabulary per Phase 6 Plan 06-06 precedent"
      pattern: "FIDO-[0-9]+ | Phase 7 | Complete"
    - from: "07-VALIDATION.md §Per-Task Verification Map"
      to: "PLAN IDs 07-01..07-07 task IDs"
      via: "One row per FIDO with Task ID, Wave, Automated Command, Status"
      pattern: "7-07-"
    - from: "KeyAuthTests/CodeApprovalViewTests.swift"
      to: "App/Views/CodeApprovalView.swift source"
      via: "loadBundledSource(named:) helper + contains(...) / NOT contains"
      pattern: "startAutoRefresh"
---

<objective>
Close Phase 7 with: (1) a grep-based `CodeApprovalViewTests.swift` completing automated coverage for FIDO-08 and FIDO-13, (2) flipping the 19 FIDO rows in REQUIREMENTS.md Traceability from "Pending" to "Complete (automated)" / "Complete (unit) / Manual QA pending" per Phase 6's hybrid vocabulary, (3) populating the VALIDATION.md Per-Task Verification Map, aligning the §Wave 0 Requirements fixture filename to the shipped `CodeRequestFixtures.swift`, and setting `nyquist_compliant: true`, (4) creating the 07-QA-CHECKLIST.md with the two manual QA items, and (5) touching STATE.md with the phase-7 position + decisions summary.

Purpose: Without this plan, Phase 7's automated coverage is present but REQUIREMENTS.md still says everything is "Pending"; the verification map and manual-QA checklist don't exist; VALIDATION.md still cites a placeholder fixture filename (`TrustWindowFixtures.swift`) that was consciously replaced with `CodeRequestFixtures.swift`; and the phase is not formally signed off.

Output: 5 file edits/creations. Full test suite remains green. Phase 7 Conditional Pass status is possible once the manual QA items in 07-QA-CHECKLIST.md are signed off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-faceid-capability-tokens/07-CONTEXT.md
@.planning/phases/07-faceid-capability-tokens/07-RESEARCH.md
@.planning/phases/07-faceid-capability-tokens/07-VALIDATION.md
@.planning/phases/06-icloud-keychain-sync/06-06-PLAN.md
@KeyAuthTests/SettingsViewTests.swift
@App/Views/CodeApprovalView.swift

<interfaces>
<!-- 1. KeyAuthTests/CodeApprovalViewTests.swift (NEW) — FIDO-08 + FIDO-13 grep tests. -->
```swift
import XCTest
@testable import KeyAuth

/// Phase 7 Plan 07-08 grep-based regression tests for CodeApprovalView.swift.
///
/// Strategy: `App/Views/CodeApprovalView.swift` is copied into the test bundle as
/// `CodeApprovalView.swift.txt` by the "Copy Shared Sources For Isolation Tests" Run-Script
/// phase (extended in Plan 07-05 Task 3). We grep the bundled source for:
///   - FIDO-08: the `TrustWindowManager.shared.mint()` call appears AFTER the
///     authenticate-success guard (regression against accidentally moving it above
///     the `guard success else { return }` block).
///   - FIDO-13: no remaining `startAutoRefresh` reference — the 5-minute Timer is gone.
@MainActor
final class CodeApprovalViewTests: XCTestCase {

    /// Loads the bundled `CodeApprovalView.swift.txt` resource from the test bundle.
    /// Mirrors the helper in SettingsViewTests.
    private func loadBundledSource(named name: String) throws -> String {
        let bundle = Bundle(for: Self.self)
        let base = (name as NSString).deletingPathExtension
        guard let url = bundle.url(forResource: base, withExtension: "swift.txt") else {
            XCTFail("Bundled source not found: \(base).swift.txt — Run-Script misconfigured?")
            return ""
        }
        return try String(contentsOf: url, encoding: .utf8)
    }

    // FIDO-08: mint call appears AFTER authenticate success, BEFORE Task.sleep dismissal
    func testMintCallAppearsAfterAuthenticateSuccess() throws {
        let src = try loadBundledSource(named: "CodeApprovalView.swift")
        XCTAssertTrue(src.contains("TrustWindowManager.shared.mint()"),
            "FIDO-08: approveAndSend must call TrustWindowManager.shared.mint()")

        // Order sanity: the mint call must come AFTER `guard success else { return }` and AFTER
        // `sendEncryptedCode`. We assert by comparing NSString range locations.
        let ns = src as NSString
        let guardRange = ns.range(of: "guard success else")
        let sendRange = ns.range(of: "RelayClient.shared.sendEncryptedCode")
        let mintRange = ns.range(of: "TrustWindowManager.shared.mint()")

        XCTAssertNotEqual(guardRange.location, NSNotFound, "authenticate-success guard missing")
        XCTAssertNotEqual(sendRange.location, NSNotFound, "sendEncryptedCode call missing")
        XCTAssertNotEqual(mintRange.location, NSNotFound, "mint() call missing")

        XCTAssertGreaterThan(mintRange.location, guardRange.location,
            "FIDO-08: mint() must appear AFTER the `guard success else { return }` line")
        XCTAssertGreaterThan(mintRange.location, sendRange.location,
            "FIDO-08: mint() must appear AFTER sendEncryptedCode (so we only mint after the send is issued)")
    }

    // FIDO-13: startAutoRefresh is fully deleted from CodeApprovalView
    func testStartAutoRefreshIsAbsent() throws {
        let src = try loadBundledSource(named: "CodeApprovalView.swift")
        XCTAssertFalse(src.contains("startAutoRefresh"),
            "FIDO-13 / D-12: CodeApprovalView.swift must NOT contain any reference to startAutoRefresh (the 5-minute Timer was deleted in Plan 07-05)")
    }
}
```

<!-- 2. REQUIREMENTS.md Traceability section — status flip summary. Use the Phase 6 hybrid vocabulary verbatim. -->
Per Phase 6 STATE.md lines 125-128, the vocabulary is:
- "Complete (automated)" — the FIDO has a green automated test asserting the behavior end-to-end
- "Complete (unit: <TestClass>.<testName>) / Manual QA pending <2-DEV-NN>" — the FIDO has unit-level coverage but end-to-end behavior requires manual QA (matches ICLOUD-10 / ICLOUD-16 precedent)
- "Manual QA pending <2-DEV-NN>" — no automated coverage exists; only manual QA

Expected Phase 7 row flips (mapping):

| FIDO | New Status |
|------|-----------|
| FIDO-01 | Complete (automated) — TrustWindowManagerTests.testInitialState_isInWindowIsFalse |
| FIDO-02 | Complete (automated) — TrustWindowManagerTests.testMintSetsExpiryTo120sFromNow |
| FIDO-03 | Complete (automated) — TrustWindowManagerTests.testMintNoOpWhenPreferenceDisabled |
| FIDO-04 | Complete (automated) — TrustWindowManagerTests.testReMintReplacesExpiry |
| FIDO-05 | Complete (automated) — TrustWindowManagerTests.testIsInWindowLazyExpiryCheck |
| FIDO-06 | Complete (automated) — TrustWindowManagerTests.testBackgroundNotificationRevokes |
| FIDO-07 | Complete (automated) — TrustWindowManagerTests.testICloudAccountChangeRevokes |
| FIDO-08 | Complete (automated) — CodeApprovalViewTests.testMintCallAppearsAfterAuthenticateSuccess |
| FIDO-09 | Complete (automated) — RelayClientSilentSendTests.testSilentSendInWindow |
| FIDO-10 | Complete (automated) — RelayClientSilentSendTests.testAmbiguousResolutionSetsPendingCodeRequest |
| FIDO-11 | Complete (automated) — TrustWindowManagerTests.testToastTextForMatchedIssuer / testToastTextFallbackEmpty |
| FIDO-12 | Complete (automated) — TrustWindowManagerTests.testToastAutoDismissAfter2s |
| FIDO-13 | Complete (automated) — CodeApprovalViewTests.testStartAutoRefreshIsAbsent |
| FIDO-14 | Complete (automated) — TrustWindowPreferenceTests.testSetEnabledPersistsInUserDefaults |
| FIDO-15 | Complete (automated) — SettingsViewTests.testTrustWindowToggleLabelMatchesUISpec (+ 2 siblings) |
| FIDO-16 | Complete (automated) — TrustWindowPreferenceTests.testBootstrapDefaultsToEnabled |
| FIDO-17 | Complete (automated) — TrustWindowManagerTests.testSingletonStateIsNotPersisted |
| FIDO-18 | Manual QA pending 2-DEV-TW-01 |
| FIDO-19 | Manual QA pending 2-DEV-TW-02 |

<!-- 3. 07-QA-CHECKLIST.md (NEW) — two manual items. -->
```markdown
# Phase 7 QA Checklist

**Created:** 2026-04-19
**Scope:** Manual QA items not covered by automated tests. Owner: solo developer (Yashesh).

## 2-DEV-TW-01 — Toast visible above ContentView after sheet dismisses (FIDO-18)

**Preconditions:**
- Physical iPhone with FaceID enabled OR iOS Simulator with "Matching Face" toggled ON
- Chrome extension paired (Phase 1-3 flow complete)
- Phase 7 toggle is ON (default — verify by opening Settings and confirming "Allow 2-minute trust window after FaceID" shows ON)

**Steps:**
1. From Chrome extension, click "Request Code" for a paired account.
2. iOS app foregrounds, `CodeApprovalView` sheet appears.
3. Tap "Approve" — confirm FaceID succeeds (simulator: Features → Face ID → Matching Face).
4. Sheet auto-dismisses after ~1.5s. Note the time on a stopwatch.
5. WITHIN 2 MINUTES of step 3, request a second code from Chrome extension.
6. Observe: NO FaceID prompt appears; a toast capsule reading "Code sent for <issuer>" appears at the top of ContentView, above the safe-area notch, and fades out after ~2 seconds.

**Pass criteria:**
- [ ] Step 4 — first request goes through FaceID flow
- [ ] Step 6 — second request is silent (no FaceID prompt, no sheet)
- [ ] Step 6 — toast is visible above ContentView, with paperplane icon, readable in both Light and Dark mode
- [ ] Toast auto-dismisses in approximately 2 seconds

**Fail signals:**
- Second request still prompts FaceID → Plan 07-06 resolver wiring is broken OR Plan 07-03 `isInWindow` gate is broken
- Toast never appears → Plan 07-07 overlay is misattached OR `TrustWindowManager.pendingToast` is not wired
- Toast appears but covered by the sheet → Plan 07-07 overlay is applied below the `.sheet` level (should be at the same level)

## 2-DEV-TW-02 — Chrome extension source unchanged (FIDO-19)

**Preconditions:**
- Phase 7 branch checked out locally
- `main` branch represents pre-Phase-7 state

**Steps:**
1. Run: `git diff main...HEAD -- extension/`
2. Observe the output.

**Pass criteria:**
- [ ] The diff output is empty (zero lines of change under `extension/`)

**Fail signals:**
- Any file under `extension/` was modified — Phase 7 is iOS-only per CONTEXT.md D-15. Revert those changes before merging.

## 2-DEV-TW-03 (optional) — Settings toggle flips preference without dialog

**Preconditions:**
- Phase 7 Settings surface deployed

**Steps:**
1. Open Settings.
2. Observe "Security" section with "Allow 2-minute trust window after FaceID" toggle.
3. Flip OFF.
4. Navigate back to main screen and then back to Settings — confirm toggle is still OFF.
5. Flip ON. Confirm it persists.

**Pass criteria:**
- [ ] No confirmation dialog on flip (unlike Phase 6 Sync toggle OFF which DOES prompt)
- [ ] State persists across navigation and app re-launch

**Notes:** This is documented as covered by `testSetEnabledPersistsInUserDefaults` at the unit level; this manual walkthrough is belt-and-suspenders for UI behavior (no dialog).
```

<!-- 4. STATE.md — add a Phase 7 position + decisions summary block. -->
Update frontmatter: `stopped_at: Phase 7 complete — ready for manual QA`. Update Current Position section. Append Phase 7 decisions to the Accumulated Decisions list (verbatim from the 17 D-NN lines in CONTEXT.md — match the Phase 6 style of one-line decision entries).

<!-- 5. 07-VALIDATION.md — replace the sample row with the full map, rewrite the Wave 0 fixture bullet, and flip frontmatter. -->
Replace the `| 7-01-01 | 01 | 1 | FIDO-01 | T-7-01 | TBD | unit | TBD | ❌ W0 | ⬜ pending |` sample row with the full 19-row Per-Task Verification Map using the FIDO → Plan → Task → Test Command mapping from the interfaces block above.

Rewrite the §Wave 0 Requirements fixture bullet (line 55) from:
```
- [ ] `KeyAuthTests/Fixtures/TrustWindowFixtures.swift` — shared fixtures (mock clock, mock ICloudStateObserver)
```
to:
```
- [x] KeyAuthTests/Fixtures/CodeRequestFixtures.swift — shared CodeRequest factory (mock clock and ICloudStateObserver mocking are handled inline via injected closures + existing DEBUG hooks; no dedicated fixture file needed for those)
```
(This aligns VALIDATION.md with the filename actually shipped by Plan 07-01 Task 2 — `CodeRequestFixtures.swift`, which more accurately describes its contents. The mock-clock seam lives on `TrustWindowManager.now` and the `ICloudStateObserver` mocking uses the existing `_primeAsSignedIn` / `_simulateIdentityChange` DEBUG hooks from Phase 6 — neither needs a separate fixture file.)

Also flip the frontmatter:
```yaml
status: complete
nyquist_compliant: true
wave_0_complete: true
```
</interfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| REQUIREMENTS.md Traceability | Planning-document integrity only — not a runtime boundary. |
| QA checklist | Human-in-the-loop signoff; no executable trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-TS3 | Tampering | Traceability rows flipped to Complete without actual test coverage | mitigate | Each row flip cites a concrete XCTest method name; Task 3's verify command runs `xcodebuild test -only-testing:KeyAuthTests` and confirms pass. If the cited tests don't exist or fail, the task fails. |
| T-7-TS4 | Repudiation | Manual-QA items hidden — FIDO-18 / FIDO-19 treated as "done" without QA | mitigate | Traceability rows explicitly read "Manual QA pending 2-DEV-TW-01 / 02" so the phase does NOT auto-flip to complete; 07-QA-CHECKLIST.md is the operational doc for the human signoff. ROADMAP Phase 7 row flips to `[x]` only after both manual items are signed off. |
| T-7-TS5 | Tampering | `Last updated:` date remains as a placeholder like `2026-04-XX`, allowing unnoticed drift between Phase 7 and future phase edits | mitigate | Task 2 writes the concrete date `2026-04-19` directly (no placeholder) and its verify command includes `! grep -q "2026-04-XX" .planning/REQUIREMENTS.md` to block any unreplaced placeholder from passing. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create CodeApprovalViewTests.swift with FIDO-08 + FIDO-13 grep assertions</name>
  <files>KeyAuthTests/CodeApprovalViewTests.swift, KeyAuth.xcodeproj/project.pbxproj</files>
  <read_first>
    - App/Views/CodeApprovalView.swift (from Plan 07-05 — confirm `TrustWindowManager.shared.mint()` present, `startAutoRefresh` absent)
    - KeyAuthTests/SettingsViewTests.swift (analog — `loadBundledSource(named:)` helper is the same pattern)
    - .planning/phases/07-faceid-capability-tokens/07-VALIDATION.md lines 697-702 (FIDO-08 and FIDO-13 test type + command)
    - .planning/phases/07-faceid-capability-tokens/07-PATTERNS.md lines 795-802 (grep strategy + Run-Script dependency)
    - KeyAuth.xcodeproj/project.pbxproj (from Plan 07-05 Task 3 — confirm `CodeApprovalView.swift.txt` is copied into the bundle)
  </read_first>
  <action>
    1. Create `KeyAuthTests/CodeApprovalViewTests.swift` VERBATIM from the full `<interfaces>` code block above. The file:
       - Declares `@MainActor final class CodeApprovalViewTests: XCTestCase`
       - Has its own `loadBundledSource(named:)` helper mirroring `SettingsViewTests.swift` lines 25-38 (or extract a shared helper — optional; inlining is acceptable for this plan)
       - Two test methods:
         - `testMintCallAppearsAfterAuthenticateSuccess` — asserts that `TrustWindowManager.shared.mint()` appears in the source AND after both `guard success else` and `RelayClient.shared.sendEncryptedCode`
         - `testStartAutoRefreshIsAbsent` — asserts the source contains zero occurrences of `startAutoRefresh`

    2. Wire the file into the `KeyAuthTests` target in `KeyAuth.xcodeproj/project.pbxproj`:
       ```bash
       ruby -r xcodeproj - <<'EOF'
       require 'xcodeproj'
       project = Xcodeproj::Project.open('KeyAuth.xcodeproj')
       tests = project.targets.find { |t| t.name == 'KeyAuthTests' } or abort "KeyAuthTests missing"
       root = project.main_group.find_subpath('KeyAuthTests', true)
       ref = root.files.find { |f| f.path == 'CodeApprovalViewTests.swift' } \
         || root.new_reference('CodeApprovalViewTests.swift')
       unless tests.source_build_phase.files.any? { |bf| bf.file_ref == ref }
         tests.source_build_phase.add_file_reference(ref)
       end
       project.save
       puts "Wired CodeApprovalViewTests.swift into KeyAuthTests target"
       EOF
       ```

    3. Run the new class: `xcodebuild test -project KeyAuth.xcodeproj -scheme KeyAuth -destination 'platform=iOS Simulator,name=iPhone 16,OS=latest' -only-testing:KeyAuthTests/CodeApprovalViewTests 2>&1 | tail -15`. Both methods must pass. (If `testMintCallAppearsAfterAuthenticateSuccess` fails on an order assertion, the Plan 07-05 edit placed `mint()` in the wrong place — revisit that plan's changes before proceeding.)
  </action>
  <verify>
    <automated>test -f KeyAuthTests/CodeApprovalViewTests.swift && grep -q "final class CodeApprovalViewTests: XCTestCase" KeyAuthTests/CodeApprovalViewTests.swift && grep -q "testMintCallAppearsAfterAuthenticateSuccess" KeyAuthTests/CodeApprovalViewTests.swift && grep -q "testStartAutoRefreshIsAbsent" KeyAuthTests/CodeApprovalViewTests.swift && grep -q "CodeApprovalViewTests.swift" KeyAuth.xcodeproj/project.pbxproj && xcodebuild test -project KeyAuth.xcodeproj -scheme KeyAuth -destination 'platform=iOS Simulator,name=iPhone 16,OS=latest' -only-testing:KeyAuthTests/CodeApprovalViewTests 2>&1 | tail -5 | grep -q "\*\* TEST SUCCEEDED \*\*"</automated>
  </verify>
  <done>
    - File `KeyAuthTests/CodeApprovalViewTests.swift` exists with both grep tests
    - File wired into KeyAuthTests target in project.pbxproj
    - `xcodebuild test -only-testing:KeyAuthTests/CodeApprovalViewTests` exits 0 with both tests passing
  </done>
</task>

<task type="auto">
  <name>Task 2: Flip REQUIREMENTS.md Traceability rows for FIDO-01..FIDO-19 to the final statuses</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (from Plan 07-01 Task 1 — confirm 19 "FIDO-NN | Phase 7 | Pending" rows exist)
    - .planning/STATE.md lines 125-128 (Phase 6 precedent for the hybrid vocabulary — "Complete (automated)" / "Complete (unit: X.Y) / Manual QA pending 2-DEV-NN")
    - Full test suite output showing all Phase 7 automated tests currently green (if any fail, do NOT flip the corresponding row)
  </read_first>
  <action>
    Edit `.planning/REQUIREMENTS.md`. Find each `| FIDO-NN | Phase 7 | Pending |` row and replace `Pending` with the final status from the mapping table in the `<interfaces>` block. Use this text verbatim for each row:

    | FIDO-01 | Phase 7 | Complete (automated) |
    | FIDO-02 | Phase 7 | Complete (automated) |
    | FIDO-03 | Phase 7 | Complete (automated) |
    | FIDO-04 | Phase 7 | Complete (automated) |
    | FIDO-05 | Phase 7 | Complete (automated) |
    | FIDO-06 | Phase 7 | Complete (automated) |
    | FIDO-07 | Phase 7 | Complete (automated) |
    | FIDO-08 | Phase 7 | Complete (automated) |
    | FIDO-09 | Phase 7 | Complete (automated) |
    | FIDO-10 | Phase 7 | Complete (automated) |
    | FIDO-11 | Phase 7 | Complete (automated) |
    | FIDO-12 | Phase 7 | Complete (automated) |
    | FIDO-13 | Phase 7 | Complete (automated) |
    | FIDO-14 | Phase 7 | Complete (automated) |
    | FIDO-15 | Phase 7 | Complete (automated) |
    | FIDO-16 | Phase 7 | Complete (automated) |
    | FIDO-17 | Phase 7 | Complete (automated) |
    | FIDO-18 | Phase 7 | Manual QA pending 2-DEV-TW-01 |
    | FIDO-19 | Phase 7 | Manual QA pending 2-DEV-TW-02 |

    Before flipping each row, re-run the cited test to ensure it actually passes. If any of the cited tests do NOT exist or are failing, leave the row as `Pending` and log the issue in the SUMMARY document — do NOT flip a Pending row to Complete without test evidence.

    After the flips, update the REQUIREMENTS.md `Last updated:` line at the bottom to the following EXACT text (no placeholder; the date is the actual current date — copy verbatim):
    ```
    *Last updated: 2026-04-19 — Phase 7 automated coverage complete (17 of 19 FIDO-NN `Complete (automated)`; FIDO-18 and FIDO-19 remain `Manual QA pending` for 2-DEV-TW-01 and 2-DEV-TW-02 respectively).*
    ```

    Do NOT modify any rows under ICLOUD-NN, RELAY-NN, etc. — Phase 6 and earlier phases are out of scope for this plan.

    Also do NOT flip the `| 7. FaceID Capability Tokens | 0/TBD | Not planned | - |` tracking row at the bottom of ROADMAP.md — the execute-phase workflow handles that row after plans are verified.
  </action>
  <verify>
    <automated>[ "$(grep -c '| FIDO-.* | Phase 7 | Complete (automated) |' .planning/REQUIREMENTS.md)" -eq 17 ] && grep -q "| FIDO-18 | Phase 7 | Manual QA pending 2-DEV-TW-01 |" .planning/REQUIREMENTS.md && grep -q "| FIDO-19 | Phase 7 | Manual QA pending 2-DEV-TW-02 |" .planning/REQUIREMENTS.md && ! grep -q "| FIDO-.* | Phase 7 | Pending |" .planning/REQUIREMENTS.md && grep -q "Last updated: 2026-04-19 — Phase 7 automated coverage complete" .planning/REQUIREMENTS.md && ! grep -q "2026-04-XX" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
    - 17 of 19 FIDO rows flipped to `Complete (automated)`
    - FIDO-18 flipped to `Manual QA pending 2-DEV-TW-01`
    - FIDO-19 flipped to `Manual QA pending 2-DEV-TW-02`
    - Zero remaining `FIDO-NN | Phase 7 | Pending` rows
    - `Last updated:` line reads the literal date `2026-04-19` (no `2026-04-XX` placeholder anywhere in the file)
  </done>
</task>

<task type="auto">
  <name>Task 3: Populate 07-VALIDATION.md Per-Task Verification Map, rewrite Wave 0 fixture bullet, and flip compliance flags</name>
  <files>.planning/phases/07-faceid-capability-tokens/07-VALIDATION.md</files>
  <read_first>
    - .planning/phases/07-faceid-capability-tokens/07-VALIDATION.md (current state — has a sample row at line 44 to be replaced; §Wave 0 Requirements line 55 still cites placeholder `TrustWindowFixtures.swift`)
    - All 7 prior plans (07-01..07-07) for accurate Task ID → FIDO mapping
    - The Phase 7 test files (TrustWindowManagerTests, TrustWindowPreferenceTests, RelayClientSilentSendTests, SettingsViewTests, CodeApprovalViewTests) for the precise `xcodebuild test -only-testing:...` commands
    - KeyAuthTests/Fixtures/CodeRequestFixtures.swift (from Plan 07-01 Task 2 — the shipped fixture filename that VALIDATION.md must reflect)
  </read_first>
  <action>
    Three coordinated edits in `.planning/phases/07-faceid-capability-tokens/07-VALIDATION.md`:

    **Edit A — Rewrite the `## Per-Task Verification Map` section.** Delete the single sample row at line 44 and replace it with 19 rows (one per FIDO) using this exact format:

    ```
    | Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
    |---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
    | 7-03-01 | 03 | 2 | FIDO-01 | T-7-07b | isInWindow=false on init | unit | xcodebuild test -only-testing:KeyAuthTests/TrustWindowManagerTests/testInitialState_isInWindowIsFalse | ✅ | ✅ pass |
    | 7-03-02 | 03 | 2 | FIDO-02 | — | mint sets 120s expiry | unit | xcodebuild test -only-testing:KeyAuthTests/TrustWindowManagerTests/testMintSetsExpiryTo120sFromNow | ✅ | ✅ pass |
    | 7-03-03 | 03 | 2 | FIDO-03 | T-7-06 | mint no-op when pref OFF | unit | xcodebuild test -only-testing:KeyAuthTests/TrustWindowManagerTests/testMintNoOpWhenPreferenceDisabled | ✅ | ✅ pass |
    | 7-03-04 | 03 | 2 | FIDO-04 | — | re-mint replaces (fresh 120s) | unit | xcodebuild test -only-testing:KeyAuthTests/TrustWindowManagerTests/testReMintReplacesExpiry | ✅ | ✅ pass |
    | 7-03-05 | 03 | 2 | FIDO-05 | — | lazy expiry check | unit | xcodebuild test -only-testing:KeyAuthTests/TrustWindowManagerTests/testIsInWindowLazyExpiryCheck | ✅ | ✅ pass |
    | 7-03-06 | 03 | 2 | FIDO-06 | T-7-03 | background revokes | unit | xcodebuild test -only-testing:KeyAuthTests/TrustWindowManagerTests/testBackgroundNotificationRevokes | ✅ | ✅ pass |
    | 7-03-07 | 03 | 2 | FIDO-07 | T-7-04 | iCloud change revokes | unit | xcodebuild test -only-testing:KeyAuthTests/TrustWindowManagerTests/testICloudAccountChangeRevokes | ✅ | ✅ pass |
    | 7-08-01 | 08 | 5 | FIDO-08 | T-7-10 | mint after authenticate | grep | xcodebuild test -only-testing:KeyAuthTests/CodeApprovalViewTests/testMintCallAppearsAfterAuthenticateSuccess | ✅ | ✅ pass |
    | 7-04-01 | 04 | 3 | FIDO-09 | T-7-01 | silent send in window | unit | xcodebuild test -only-testing:KeyAuthTests/RelayClientSilentSendTests/testSilentSendInWindow | ✅ | ✅ pass |
    | 7-04-02 | 04 | 3 | FIDO-10 | — | ambiguous defers to FaceID | unit | xcodebuild test -only-testing:KeyAuthTests/RelayClientSilentSendTests/testAmbiguousResolutionSetsPendingCodeRequest | ✅ | ✅ pass |
    | 7-03-08 | 03 | 2 | FIDO-11 | — | toast text for matched issuer | unit | xcodebuild test -only-testing:KeyAuthTests/TrustWindowManagerTests/testToastTextForMatchedIssuer | ✅ | ✅ pass |
    | 7-03-09 | 03 | 2 | FIDO-12 | — | toast auto-dismiss 2s | unit | xcodebuild test -only-testing:KeyAuthTests/TrustWindowManagerTests/testToastAutoDismissAfter2s | ✅ | ✅ pass |
    | 7-08-02 | 08 | 5 | FIDO-13 | T-7-13 | startAutoRefresh deleted | grep | xcodebuild test -only-testing:KeyAuthTests/CodeApprovalViewTests/testStartAutoRefreshIsAbsent | ✅ | ✅ pass |
    | 7-02-01 | 02 | 1 | FIDO-14 | T-7-06 | setEnabled persists | unit | xcodebuild test -only-testing:KeyAuthTests/TrustWindowPreferenceTests/testSetEnabledPersistsInUserDefaults | ✅ | ✅ pass |
    | 7-07-01 | 07 | 4 | FIDO-15 | T-7-CR1 | toggle label verbatim | grep | xcodebuild test -only-testing:KeyAuthTests/SettingsViewTests/testTrustWindowToggleLabelMatchesUISpec | ✅ | ✅ pass |
    | 7-02-02 | 02 | 1 | FIDO-16 | — | bootstrap defaults ON | unit | xcodebuild test -only-testing:KeyAuthTests/TrustWindowPreferenceTests/testBootstrapDefaultsToEnabled | ✅ | ✅ pass |
    | 7-03-10 | 03 | 2 | FIDO-17 | T-7-07b | not persisted across launches | unit | xcodebuild test -only-testing:KeyAuthTests/TrustWindowManagerTests/testSingletonStateIsNotPersisted | ✅ | ✅ pass |
    | 7-QA-01 | 08 | 5 | FIDO-18 | T-7-08 | toast visible above ContentView | manual | 07-QA-CHECKLIST.md 2-DEV-TW-01 | ⚠ QA | ⏳ pending |
    | 7-QA-02 | 08 | 5 | FIDO-19 | — | extension source unchanged | manual | 07-QA-CHECKLIST.md 2-DEV-TW-02 | ⚠ QA | ⏳ pending |
    ```

    (Task IDs 7-XX-NN reference the plan-ID . task-ordinal used by execute-phase. Exact ordinals may differ; align with actual plan task orderings when flipping. The above is the planner's best assignment — checker/executor may adjust the ordinal column.)

    **Edit B — Rewrite the §Wave 0 Requirements fixture bullet** (currently line 55 — references the never-created placeholder `TrustWindowFixtures.swift`). Replace:
    ```
    - [ ] `KeyAuthTests/Fixtures/TrustWindowFixtures.swift` — shared fixtures (mock clock, mock ICloudStateObserver)
    ```
    with the literal text (no backticks around the path so the grep assertion below can match without escaping):
    ```
    - [x] KeyAuthTests/Fixtures/CodeRequestFixtures.swift — shared CodeRequest factory (mock clock and ICloudStateObserver mocking are handled inline via injected closures + existing DEBUG hooks; no dedicated fixture file needed for those)
    ```

    While here, flip the three other §Wave 0 Requirements bullets (TrustWindowManagerTests, RelayClientSilentSendTests, TrustWindowPreferenceTests) from `- [ ]` to `- [x]` since Plan 07-01 Task 2 created all of them and Plans 02/03/04 filled their assertions.

    **Edit C — Flip the VALIDATION.md frontmatter:**
    ```yaml
    ---
    phase: 7
    slug: faceid-capability-tokens
    status: complete
    nyquist_compliant: true
    wave_0_complete: true
    created: 2026-04-19
    ---
    ```

    Update the Validation Sign-Off checklist at the bottom — flip all 6 items to `- [x]` and change `Approval: pending` to `Approval: complete`.
  </action>
  <verify>
    <automated>grep -q "nyquist_compliant: true" .planning/phases/07-faceid-capability-tokens/07-VALIDATION.md && grep -q "status: complete" .planning/phases/07-faceid-capability-tokens/07-VALIDATION.md && [ "$(grep -c 'FIDO-' .planning/phases/07-faceid-capability-tokens/07-VALIDATION.md)" -gt 19 ] && grep -q "2-DEV-TW-01" .planning/phases/07-faceid-capability-tokens/07-VALIDATION.md && grep -q "2-DEV-TW-02" .planning/phases/07-faceid-capability-tokens/07-VALIDATION.md && grep -q "Approval: complete" .planning/phases/07-faceid-capability-tokens/07-VALIDATION.md && grep -q "CodeRequestFixtures.swift" .planning/phases/07-faceid-capability-tokens/07-VALIDATION.md && ! grep -q "TrustWindowFixtures.swift" .planning/phases/07-faceid-capability-tokens/07-VALIDATION.md</automated>
  </verify>
  <done>
    - Per-Task Verification Map has 19 rows (17 automated + 2 manual)
    - Frontmatter `nyquist_compliant: true` and `status: complete`
    - §Wave 0 Requirements fixture bullet now references `CodeRequestFixtures.swift` (the shipped filename); the `TrustWindowFixtures.swift` placeholder is gone from the document
    - Wave 0 checklist flipped to all `[x]`
    - Validation Sign-Off flipped to all `[x]` and `Approval: complete`
  </done>
</task>

<task type="auto">
  <name>Task 4: Create 07-QA-CHECKLIST.md with the two manual QA items</name>
  <files>.planning/phases/07-faceid-capability-tokens/07-QA-CHECKLIST.md</files>
  <read_first>
    - .planning/phases/06-icloud-keychain-sync/06-QA-CHECKLIST.md (if it exists — analog format)
    - .planning/phases/07-faceid-capability-tokens/07-UI-SPEC.md §Interaction Patterns toast sections (source of the pass/fail observations)
    - .planning/phases/07-faceid-capability-tokens/07-CONTEXT.md D-15 (extension source unchanged rationale)
  </read_first>
  <action>
    Create `.planning/phases/07-faceid-capability-tokens/07-QA-CHECKLIST.md` VERBATIM from the full markdown block in the `<interfaces>` section above. The file must contain:

    - Title `# Phase 7 QA Checklist`
    - Metadata block (Created: 2026-04-19; Scope; Owner)
    - `## 2-DEV-TW-01 — Toast visible above ContentView after sheet dismisses (FIDO-18)` with 6 steps and 4 pass criteria checkboxes
    - `## 2-DEV-TW-02 — Chrome extension source unchanged (FIDO-19)` with 2 steps and 1 pass criterion
    - `## 2-DEV-TW-03 (optional) — Settings toggle flips preference without dialog` with 5 steps and 2 pass criteria

    Use the exact pass/fail criteria from the interfaces block. Do NOT add additional items — Phase 7's manual QA surface is deliberately limited to these three cases (the third is optional).
  </action>
  <verify>
    <automated>test -f .planning/phases/07-faceid-capability-tokens/07-QA-CHECKLIST.md && grep -q "2-DEV-TW-01" .planning/phases/07-faceid-capability-tokens/07-QA-CHECKLIST.md && grep -q "2-DEV-TW-02" .planning/phases/07-faceid-capability-tokens/07-QA-CHECKLIST.md && grep -q "git diff main...HEAD -- extension/" .planning/phases/07-faceid-capability-tokens/07-QA-CHECKLIST.md && grep -q "FIDO-18" .planning/phases/07-faceid-capability-tokens/07-QA-CHECKLIST.md && grep -q "FIDO-19" .planning/phases/07-faceid-capability-tokens/07-QA-CHECKLIST.md</automated>
  </verify>
  <done>
    - File exists at `.planning/phases/07-faceid-capability-tokens/07-QA-CHECKLIST.md`
    - Contains both 2-DEV-TW-01 and 2-DEV-TW-02 sections with steps + pass criteria
    - The optional 2-DEV-TW-03 is included but clearly marked optional
  </done>
</task>

<task type="auto">
  <name>Task 5: Update STATE.md with Phase 7 position + decisions summary; run full suite sanity check</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (current file — update frontmatter, Current Position, Accumulated Decisions)
    - .planning/phases/07-faceid-capability-tokens/07-CONTEXT.md (17 D-NN decisions — source of the Phase 7 decisions block entries)
  </read_first>
  <action>
    Three edits to `.planning/STATE.md`:

    1. Update frontmatter block:
       - `stopped_at: Phase 7 complete — ready for manual QA`
       - `last_updated: 2026-04-19` (actual UTC date)
       - `last_activity: 2026-04-19`
       - `progress.total_phases: 7` (unchanged; already counts Phase 7)
       - `progress.completed_phases: 4` (up from 3 — Phase 6 is still "manual QA pending" but Phase 7 follows the same pattern)
       - NOTE: if the current STATE.md already treats "completed" as "automated coverage green" regardless of manual QA, increment. If it treats "completed" as strictly "fully signed off", leave at 3 and add a note. Match the existing convention — do not change the accounting rules unilaterally.

    2. Update the `## Current Position` section:
       ```
       Phase: 07-faceid-capability-tokens — Conditional Pass (manual QA pending)
       Plan: 8 of 8
       Status: Phase complete — ready for manual QA (2-DEV-TW-01, 2-DEV-TW-02)
       Last activity: 2026-04-19
       ```

    3. Append Phase 7 decisions to the `### Decisions` list, one per D-NN — mirror the Phase 6 one-line entry format (already established at lines 104-128). Example entries:
       - `- [Phase 07-faceid-capability-tokens]: D-01 — 2-minute trust window opens after FaceID success; subsequent requests from paired extension silent-send`
       - `- [Phase 07-faceid-capability-tokens]: D-02 — Global-per-pairing scope (no per-account/per-origin refinement); user explicitly chose simplicity`
       - `- [Phase 07-faceid-capability-tokens]: D-03 — TTL is fixed-from-mint (2 min); uses do NOT extend the window`
       - `- [Phase 07-faceid-capability-tokens]: D-12 — Deleted CodeApprovalView.startAutoRefresh 5-minute Timer (request-driven model replaces it)`
       - `- [Phase 07-faceid-capability-tokens]: D-14 — Accepted phishing-origin replay risk within 2-min window; visible toast (D-09) is the user-awareness mitigation`
       - `- [Phase 07-faceid-capability-tokens]: D-16 — Trust-window preference defaults ON for all users`
       - ... (include the other 11 D-NN decisions in similar format, keeping each line under ~120 chars)

       If appending all 17 is too verbose, compress to the 6 most consequential (D-01, D-02, D-03, D-12, D-14, D-16) — matching the Phase 6 style which lists only the load-bearing decisions, not all of them. Use your judgment and document the criterion in the plan SUMMARY.

    4. Update the performance metrics table at the bottom of the table (lines 55-68) by appending one row per Plan 07 execution (Plan 07-01 through 07-08). The executor will update these during plan execution; this plan only needs to ensure the table structure is ready. If Plan executions haven't happened yet by the time this plan runs (e.g., testing mode), leave rows as estimates or blank but keep the table header.

    After the edits, run `xcodebuild test -project KeyAuth.xcodeproj -scheme KeyAuth -destination 'platform=iOS Simulator,name=iPhone 16,OS=latest' -only-testing:KeyAuthTests 2>&1 | tail -10`. The full KeyAuthTests suite must exit 0. This is the sanity check that Plans 01-08 together leave the project in a working state.
  </action>
  <verify>
    <automated>grep -q "07-faceid-capability-tokens" .planning/STATE.md && grep -q "2-DEV-TW-01" .planning/STATE.md && grep -q "Phase 7 complete" .planning/STATE.md && xcodebuild test -project KeyAuth.xcodeproj -scheme KeyAuth -destination 'platform=iOS Simulator,name=iPhone 16,OS=latest' -only-testing:KeyAuthTests 2>&1 | tail -5 | grep -q "\*\* TEST SUCCEEDED \*\*"</automated>
  </verify>
  <done>
    - STATE.md frontmatter reflects Phase 7 completion (with manual QA pending)
    - Current Position section names Phase 7 as current
    - Accumulated Decisions list includes at least 6 Phase 7 D-NN entries
    - Full KeyAuthTests suite passes — no regression introduced by Phase 7
  </done>
</task>

</tasks>

<verification>
  Plan-level checks:
  - All 17 FIDO-NN rows (FIDO-01..FIDO-17) marked `Complete (automated)` in REQUIREMENTS.md
  - FIDO-18 / FIDO-19 marked `Manual QA pending 2-DEV-TW-01 / 02`
  - REQUIREMENTS.md `Last updated:` line contains the literal date `2026-04-19` with zero `2026-04-XX` placeholders remaining anywhere in the file
  - `CodeApprovalViewTests.swift` exists with 2 passing grep tests
  - `07-VALIDATION.md` has populated Per-Task Verification Map + `nyquist_compliant: true` + the §Wave 0 Requirements fixture bullet references `CodeRequestFixtures.swift` (no lingering `TrustWindowFixtures.swift`)
  - `07-QA-CHECKLIST.md` exists with both manual items
  - STATE.md updated with Phase 7 position + decisions
  - Full `xcodebuild test -only-testing:KeyAuthTests` exits 0 — Phase 6 + Phase 7 all green
</verification>

<success_criteria>
- Phase 7 traceability closed (automated coverage formally recorded; manual QA explicit)
- QA-CHECKLIST gives the solo developer a clear manual-verification path for FIDO-18 / FIDO-19
- VALIDATION.md Per-Task Verification Map is complete and nyquist-compliant; fixture filename matches the shipped code
- STATE.md reflects the phase's end-state and preserves Phase 7 decisions for future phase context
- Full test suite remains green — no accidental regression
- Phase 7 ROADMAP.md tracking row can flip to `[x]` once manual QA signs off the two 2-DEV-TW-NN items (that flip is handled by `/gsd-verify-work` / `/gsd-execute-phase`, not by this plan)
</success_criteria>

<output>
After completion, create `.planning/phases/07-faceid-capability-tokens/07-08-SUMMARY.md` with: (1) CodeApprovalViewTests file contents + pass log, (2) REQUIREMENTS.md traceability diff (19 `Pending` → final statuses, plus `Last updated:` line with literal 2026-04-19), (3) VALIDATION.md frontmatter + Per-Task Map + §Wave 0 fixture-bullet new-state snapshot (highlighting the `TrustWindowFixtures.swift` → `CodeRequestFixtures.swift` rename), (4) QA checklist summary (2 required + 1 optional item), (5) STATE.md diff (frontmatter + Current Position + 6 new decision entries), (6) final `xcodebuild test` pass counter — e.g., "X passed, 0 failed, Y skipped".
</output>
</content>
</invoke>
